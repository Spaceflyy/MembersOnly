<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<title><%=title%></title>
	</head>
	<%-include('./partials/header')%>
	<body>
		<% if (locals.user) {%>
		<h1>WELCOME BACK <%= user.username %></h1>
		<a href="/newmessage">Create Message</a>
		<a href="/logout">Log Out</a>
		<%-include('./partials/messages')%> <% } else { %>
		<h1>please log in</h1>
		<%-include("login") %> <%}%> <% if (locals.message) {%>
		<h1><%= locals.message%></h1>
		<% } %>
	</body>
</html>
